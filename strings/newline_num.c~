#include<stdio.h>
int main()
{
	int n,count=0;
	printf("%d\n",EOF);
	while((n=getchar()) != EOF)
	{
		if(n == '\n')
		{
			count++;
			printf("%d\n",count);
		}
	
	}
	printf("%d\n",count);
		scanf("%d",&n);
	return 0;

}
