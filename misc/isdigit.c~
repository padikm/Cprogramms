#include<stdio.h>
#include<malloc.h>
#include<stdlib.h>
#include<math.h>
#include<ctype.h>
int main()
{
	char *p;
	int n;
	p = (char *) malloc(10);
	gets(p);
	if(isdigit(*p))
	{
	 n = atoi(p);	
	
	}
	else 
	{
		return 0;
	}

	printf("%d\n",n);
	return 0;



}
