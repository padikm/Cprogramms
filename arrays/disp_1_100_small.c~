#include<stdio.h>
void disp(int *a,int ,int );
int main()
{
	int a[]={1,2,3,4};

	disp(a,3,0);
	return 0;

}

void disp(int *a,int n,int i)
{

	if(i>(n))
		return ;

	printf("%d\n",a[i++]);
	disp(a,n,i);

}
